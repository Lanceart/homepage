import React from "react";
const BlogLayout = ({children}) => {
    return(

        <div>
                {children}
                {/* <h1>Hi this is blog</h1> */}
        </div>
    );
};

export default BlogLayout;